import { mudConfig } from "@latticexyz/world/register";

export default mudConfig({
  tables: {
  },
  modules: [
    {
      name: "UniqueEntityModule",
      root: true,
      args: [],
    }
  ],
});
